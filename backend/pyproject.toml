[project]
name = "road-incidents-ingester"
version = "0.1.0"
description = "Ingester that consolidates Canadian provincial highway events into Supabase"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
